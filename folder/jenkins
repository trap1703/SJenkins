#!/bin/env groovy

node {
    deleteDir()

    checkout scm

    files = sh(returnStdout: true, script: 'ls')
    println "list of property files"
    p_files = files.split("\n").collect()[2,3]
    println (p_files)
    X_files = sh(returnStdout:true, script: 'cat file1')
    println (X_files)
    
    sh 'echo Welcome to IT!'
}
